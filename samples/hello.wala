import "wasi_unstable" "fd_write"
  func $fd_write
    param i32 i32 i32 i32
    result i32
(;

comment

;)
memory(export("memory") 1)

data((16i32) "Hello, W0rld!\n")

func $main export("_start")
  i32.store 8i32 16i32
  i32.store offset=4 8i32 14i32
  call $fd_write
    1i32 ;; fd: stdout
    8i32 ;; const ciovec*
    1i32 ;; len of ciovec
    4i32 ;; written*
  drop
