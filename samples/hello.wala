import "wasi_unstable" "fd_write"
  func $fd_write
    param i32 i32 i32 i32
    result i32
(;

comment

;)
memory(export("memory") 1)

data((16) "Hello, W0rld!\n")

func $main export("_start")
  i32.store 8 16
  i32.store offset=4 8 14
  call $fd_write
    1 ;; fd: stdout
    8 ;; const ciovec*
    1 ;; len of ciovec
    4 ;; written*
  drop
